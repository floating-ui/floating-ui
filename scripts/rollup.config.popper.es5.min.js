import deepAssign from 'deep-assign';
import popperEs5 from './rollup.config.popper.es5';
import minify from './rollup.config.minify';

const root = `${__dirname}/..`;

export default deepAssign(popperEs5, minify, {
    dest: `${root}/dist/popper.es5.min.js`,
    sourceMapFile: `${root}/dist/popper.es5.min.js.map`,
});
