import deepAssign from 'deep-assign';
import popper from './rollup.config.popper';
import minify from './rollup.config.minify';

const root = `${__dirname}/..`;

export default deepAssign(minify, popper, {
    dest: `${root}/dist/popper.min.js`,
    sourceMapFile: `${root}/dist/popper.min.js.map`,
});
