import deepAssign from 'deep-assign';
import popper from './rollup.config.popper.js';
import min from './rollup.config.min.js';

const root = `${__dirname}/..`;

export default deepAssign(popper, min, {
    dest: `${root}/build/popper.min.js`,
    sourceMapFile: `${root}/build/popper.min.js.map`,
});
