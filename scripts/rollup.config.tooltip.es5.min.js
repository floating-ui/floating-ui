import deepAssign from 'deep-assign';
import tooltipEs5 from './rollup.config.tooltip.es5';
import minify from './rollup.config.minify';

const root = `${__dirname}/..`;

export default deepAssign(tooltipEs5, minify, {
    dest: `${root}/dist/tooltip.es5.min.js`,
    sourceMapFile: `${root}/dist/tooltip.es5.min.js.map`,
});
