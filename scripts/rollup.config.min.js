import deepAssign from 'deep-assign';
import babel from 'rollup-plugin-babel';
import base from './rollup.config.base.js';

export default deepAssign(base, {
    plugins: base.plugins.concat([babel({
        presets: ['babili'],
        minified: true,
        compact: true,
        comments: false,
    })]),
});
