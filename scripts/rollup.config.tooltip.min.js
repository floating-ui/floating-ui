import deepAssign from 'deep-assign';
import tooltip from './rollup.config.tooltip';
import minify from './rollup.config.minify';

const root = `${__dirname}/..`;

export default deepAssign(minify, tooltip, {
    dest: `${root}/dist/tooltip.min.js`,
    sourceMapFile: `${root}/dist/tooltip.min.js.map`,
});
