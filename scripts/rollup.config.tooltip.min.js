import deepAssign from 'deep-assign';
import tooltip from './rollup.config.tooltip.js';
import min from './rollup.config.min.js';

const root = `${__dirname}/..`;

export default deepAssign(tooltip, min, {
    dest: `${root}/build/tooltip.min.js`,
    sourceMapFile: `${root}/build/tooltip.min.js.map`,
});
