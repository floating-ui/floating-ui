export {useFloating} from './';

export {
  arrow,
  adaptivePlacement,
  fallback,
  bestFit,
  hide,
  offset,
  shift,
  limitShift,
  size,
  inline,
  detectOverflow,
} from '@floating-ui/core';

export type {
  Platform,
  Placement,
  Strategy,
  Middleware,
} from '@floating-ui/core';
