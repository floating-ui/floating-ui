export {useFloating} from './';

export {
  arrow,
  autoPlacement,
  flip,
  hide,
  offset,
  shift,
  limitShift,
  size,
  detectOverflow,
} from '@floating-ui/core';

export type {
  Platform,
  Placement,
  Strategy,
  Middleware,
} from '@floating-ui/core';
