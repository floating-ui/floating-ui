{
  "name": "tooltip.js",
  "version": "1.1.4",
  "description": "A kickass library to create tooltips, based on Popper.js",
  "main": "./dist/umd/tooltip.js",
  "module": "./dist/esm/tooltip.js",
  "scripts": {
    "build": "popper-bundle --build=tooltip",
    "prepublish": "npm run build",
    "pretest": "npm run lint",
    "test": "popper-karma",
    "lint": "popper-lint .",
    "coverage": "NODE_ENV=coverage npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FezVrasta/popper.js.git"
  },
  "dependencies": {
    "popper.js": "^1.0.2"
  },
  "devDependencies": {
    "scripts": "^1.0.0",
    "test-utils": "^1.0.0",
    "eslint-config-popper": "^1.0.0"
  }
}
